puts: (%string) -> %int;

func: (%int, %string) -> %int = (x: %int, y: %string) -> %int { return 3 - x; };

quicksort = (list: [%int]) -> [%int] {
	lista = filter ( (x: %int) -> %int { return x == 1; }, list );
};

arr: [%int] = test[3];

quicksort(arr);

